services:
  # Web server với PHP
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/php.ini
    # depends_on:
    #   - mysql
    environment:
      - DB_HOST=host.docker.internal
      - DB_USER=langla_user
      - DB_PASSWORD=langla123
      - DB_NAME=langla
      - DB_PORT=3307
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - langla_network

  # MariaDB Database (cập nhật theo cấu hình của bạn)
  # mysql:
  #   image: mariadb:10.6
  #   container_name: langla_mysql
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root123
  #     MYSQL_DATABASE: langla
  #     MYSQL_USER: langla_user
  #     MYSQL_PASSWORD: langla123
  #   ports:
  #     - "3307:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./SQL/langla_init.sql:/docker-entrypoint-initdb.d/init.sql
  #   networks:
  #     - langla_network

  # phpMyAdmin (tùy chọn)
  # phpmyadmin:
  #   image: phpmyadmin/phpmyadmin
  #   ports:
  #     - "8081:80"
  #   environment:
  #     PMA_HOST: mysql
  #     PMA_USER: langla_user
  #     PMA_PASSWORD: langla123
  #     PMA_PORT: 3306
  #   depends_on:
  #     - mysql
  #   networks:
  #     - langla_network

volumes:
  mysql_data:

networks:
  langla_network:
    driver: bridge
